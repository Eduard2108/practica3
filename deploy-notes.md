# Notas de Despliegue Simulado - UNEMI

## Pasos para desplegar:
1. Clonar el repositorio.
2. Ejecutar `docker-compose up -d --build` en la carpeta raíz.
3. El sistema estará disponible en `http://localhost:8000`.